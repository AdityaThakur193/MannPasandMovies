{
  "name": "mannpasandmovies-mern",
  "version": "1.0.0",
  "description": "Full-stack movie discovery app with React, Node.js, Express, and MongoDB",
  "main": "index.js",
  "scripts": {
    "client": "cd client && npm run dev",
    "server": "cd server && npm run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "install-all": "npm install && cd client && npm install && cd ../server && npm install",
    "build": "cd client && npm run build"
  },
  "keywords": ["react", "mongodb", "express", "nodejs", "movies", "tmdb"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
